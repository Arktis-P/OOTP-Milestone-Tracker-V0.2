# Diagnostics

Local validation composites may be generated here to verify:

- front background + batter overlay
- front background + pitcher overlay

These are generated validation outputs and are not required by the runtime.
