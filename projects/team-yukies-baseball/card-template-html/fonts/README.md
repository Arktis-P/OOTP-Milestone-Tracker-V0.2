# Font setup

The template uses the Esamanru / 이사만루 family.

Keep the locally licensed font files in this folder for local rendering. The CSS currently checks `esamanru.woff2` and `esamanru.ttf`; adjust the @font-face sources if the local filenames differ.
